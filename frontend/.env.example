# Leave unset in development — Vite proxy forwards /api → http://localhost:8080/api (no CORS)
# Set this only for production builds pointing at a real gateway URL
# VITE_API_URL=https://api.example.com/api
